# Two tuples
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)

# Concatenation
result = tuple1 + tuple2
print("Concatenated tuple:", result)

# Length of tuple
print("Length:", len(result))

# Check element
print("Is 3 present?", 3 in result)